name: Multi-Stage CI/CD Pipeline

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

<<<<<<< HEAD
# Concurrency: only allow one workflow per branch, cancel older runs
=======
>>>>>>> bc36ffd (added multi ci-cd)
concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: false

jobs:
<<<<<<< HEAD
  # 1️⃣ Build Stage
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Run build
        run: npm run build || echo "No build script defined"

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: build
          path: |
            ./dist/
            ./build/

  # 2️⃣ Test Stage
=======
  # 1️⃣ Lint + Test
>>>>>>> bc36ffd (added multi ci-cd)
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 18
      - run: npm install
      - name: Lint
        run: npm run lint
      - name: Run tests
        run: npm run test
      - name: Upload test results (optional)
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: ./test-results/ || echo "No test-results folder"

  # 2️⃣ Security Scan
  scan:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 18
      - run: npm audit || echo "Audit finished"

  # 3️⃣ Package
  package:
    runs-on: ubuntu-latest
    needs: scan
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 18
      - run: npm install
      - run: |
          mkdir -p dist-package
          cp -r package.json dist-package/
          cp -r src dist-package/
      - uses: actions/upload-artifact@v4
        with:
          name: package
          path: dist-package/

  # 4️⃣ Release
  release:
    runs-on: ubuntu-latest
    needs: package
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 18
      - uses: actions/download-artifact@v4
        with:
          name: package
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: "dist-package/**"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
